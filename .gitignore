node_modules/
.DS_Store
resources/models/
resources/ollama/
.next/
out/
dist/
.env
.playwright-mcp/
.build-cache/
.build-stash/
scripts/.venv/

# Lab is tracked; only generated run artifacts are ignored
afchat_lab/results/
afchat_lab/results_he/
afchat_lab/results_124/
afchat_lab/judge.log
afchat_lab/.run.pid
afchat_lab/.venv/
afchat_lab/**/__pycache__/

# 124 corpus + Aristo docs = real IAF flight-manual content. Now TRACKED in this
# PRIVATE repo so the (USB-less, internet-connected) Windows dev box gets them via
# git. Generated PDF reports stay out.
afchat_lab/*.pdf

# Local Ollama wiring (symlinks to the machine's binary + model store). The model
# weights (~4.8 GB) are NOT in git — too large for free LFS; obtained per-machine.
resources/models
resources/ollama
